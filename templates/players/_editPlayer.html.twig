{% extends 'base.html.twig' %}

{% block title %}Modifier ce joueur {% endblock %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}


{% block body %}
    <div class="new-game">
        <div class='container-fluid text-white text-center pt-5'>
                <h1 class="mt-2 text-light text-center pb-3 m-5 border-bottom border-primary border-3 fs-1">Modifier ce joueur</h1>
            {{ include('players/_form.html.twig', {'button_label': 'Mettre à jour ce joueur'}) }}
            <div>{{ include('players/_deletePlayer.html.twig') }}</div>
            <div class="mt-4">
                <a class="text-light" href="{{ path('admin_show_players') }}">Retour à la liste des joueurs</a>
            </div>
        </div>
    </div>
{% endblock %}