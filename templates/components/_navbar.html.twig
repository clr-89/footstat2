
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand d-flex flex-row fs-1 me-5" href="/">Footstat <img class="foot-navbar d-none d-sm-block ms-2" src="{{ asset('build/images/ballon.png')}}"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                {% if app.user %}
                <a class="nav-link active fs-3 me-5" aria-current="page" href="{{ path('stat_players') }}">Stats générales</a>
                <a class="nav-link fs-3 me-5" href="{{ path('stat_player') }}">Stats de {{ app.user.pseudo }}
                     {% if app.user.picture %}
                        <img class="user-picture rounded-circle" src="{{ asset('/images/user/' ~ app.user.picture) }}">
                     {% endif %}
               </a>
                     {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle fs-3 me-5" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Admin
                            </a>
                               <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                   <a class="dropdown-item fs-4 me-5" href="{{ path('admin_new_game') }}"><i class="fas fa-futbol pe-2"></i>Créer un match</a>
                                   <a class="dropdown-item fs-4" href="{{ path('app_register') }}"><i class="fas fa-child pe-2"></i>Créer un joueur</a>
                                   <li><hr class="dropdown-divider"></li>
                                   <a class="dropdown-item fs-4" href="{{ path('admin_show_games') }}"><i class="fas fa-futbol pe-2"></i>Modifier un match</a>
                                   <a class="dropdown-item fs-4" href="{{ path('admin_show_players') }}"><i class="fas fa-child pe-2"></i>Modifier un joueur</a>
                                   <a class="dropdown-item fs-4" href="{{ path('admin_show_games_list') }}"><i class="fas fa-chart-area pe-2"></i>Modifier les statistiques d'un joueur</a>
                                   <li><hr class="dropdown-divider"></li>
                                </ul>
                         </li>
                     {% endif %}
                <a class="nav-link fs-3 me-5" href="{{ path('app_logout') }}"> Se déconnecter </a>
                {% else %}
                <a class="nav-link fs-3 me-5" href="{{ path('login') }}"> Se connecter <i class="fa-solid fa-user fs-4"></i></a>
                    {% endif %}
                <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true"></a>
            </div>
        </div>
    </div>
</nav>
